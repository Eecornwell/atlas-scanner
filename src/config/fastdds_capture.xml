<?xml version="1.0" encoding="UTF-8" ?>
<!-- FastDDS profile for short-lived capture processes.
     Uses UDP only (no SHM) to avoid stale SHM segment conflicts with
     long-running driver processes, while still discovering them via the
     same 192.168.1.50 interface used by fastdds_atlas.xml. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
    <transport_descriptors>
        <transport_descriptor>
            <transport_id>udp_wired_only</transport_id>
            <type>UDPv4</type>
            <interfaceWhiteList>
                <address>127.0.0.1</address>
                <address>192.168.1.50</address>
            </interfaceWhiteList>
        </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="atlas_participant" is_default_profile="true">
        <rtps>
            <userTransports>
                <transport_id>udp_wired_only</transport_id>
            </userTransports>
            <useBuiltinTransports>false</useBuiltinTransports>
        </rtps>
    </participant>
</profiles>
